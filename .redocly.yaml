rules:
  # nullable: true is OpenAPI 3.0 syntax; in 3.1 the equivalent is
  # type: [x, 'null']. We use the 3.0 form intentionally because
  # oapi-codegen does not yet fully support 3.1 union types. Treat
  # the struct warning as advisory only so lint CI passes.
  struct: warn
