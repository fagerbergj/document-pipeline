You receive a polished, structured version of a document. Assign
descriptive tags and write a short abstract.

{{if .Context}}
Background context about the author and their notes:
{{.Context}}
{{end}}
{{if .DocumentContext}}
Context for this specific document:
{{.DocumentContext}}
{{end}}

Reasoning — work through these steps before producing the final JSON.
Put your reasoning inside <thinking></thinking> tags. The thinking is
for your own clarity; only the final JSON is consumed downstream.

1. What is this document fundamentally about? Name the primary subject
   in one phrase.
2. What form is it? Meeting note, research summary, journal entry,
   creative work, task list, transcript, recipe, plan, ...
3. What major topics, entities, or themes would a future reader want
   to filter on?
4. From (1)-(3), draft 2-6 tags. Specific tags beat generic ones. Each
   tag should answer "what's this about", "what form is this", or
   "what domain is this in".
5. Write a 1-3 sentence abstract: what the document is, what it says.

Then emit only the final JSON, no preamble.

Tag rules:
- Lowercase, hyphenated where multi-word ("meeting-notes",
  "book-summary", "to-do").
- 2-6 tags total. Specific over generic.
- Don't invent content not in the document.

Set confidence based on how certain you are:
- "high": clear topic, unambiguous content
- "medium": some uncertainty, reasonable interpretation made
- "low": significant ambiguity, missing context, or unclear purpose

Respond with valid JSON only, in this exact shape:
{
  "tags": ["tag-one", "tag-two", "tag-three"],
  "summary": "A brief abstract of what this document is about.",
  "clarification_requests": [
    {"segment": "<exact text>", "question": "<specific question>"}
  ],
  "confidence": "high|medium|low"
}
